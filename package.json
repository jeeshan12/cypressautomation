{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "Cypress automation repo",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --headless --browser chrome",
    "test:dashboard": "cypress run  --record --headless --browser chrome",
    "cy:open": "cypress open",
    "posttest": "mochawesome-merge 'cypress/results/*.json' > mochawesome.json && npx marge mochawesome.json"
  },
  "keywords": [
    "cypress",
    "cypress",
    "automation",
    "frames",
    "handling"
  ],
  "author": "mjeeshan",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^6.6.0",
    "mochawesome": "^6.2.2",
    "mochawesome-merge": "^4.2.0",
    "mochawesome-report-generator": "^5.2.0"
  }
}
